{
  "name": "kleek-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ alchemy/kleek-subgraph",
    "create-local": "graph create --node http://localhost:8020/ alchemy/kleek-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ alchemy/kleek-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 alchemy/kleek-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.73.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
